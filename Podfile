# Uncomment the next line to define a global platform for your project
  platform :ios, ’10.3.0’

target 'Chat client with parser' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Chat client with parser
  pod 'Parse'

 pod 'ParseLiveQuery'

  target 'Chat client with parserTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Chat client with parserUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
